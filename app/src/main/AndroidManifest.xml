<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MoneyMate"
        tools:targetApi="31">

        <activty
            android:name=".BudgetActivity"
            android:exported="false" />
        <activity android:name=".WelcomeActivity" />

        <activty
            android:name=".SignUpActivity"
            android:exported="false" />
        <activty
            android:name=".LoginActivity"
            android:exported="false" />
        <activty
            android:name=".ViewExpensesActivity"
            android:exported="false" />
        <activty android:name=".CreateCategoryActivity" />
        <activty android:name=".TrackExpenseActivity" />
        <activty android:name=".GoalsActivity" />
        <activty android:name=".CategorySummaryActivity" />
        <activty
            android:name=".MainActivity"
            android:exported="true" />


        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </application>

</manifest>